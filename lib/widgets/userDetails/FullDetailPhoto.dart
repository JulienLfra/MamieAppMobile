import 'package:flutter/material.dart';

class FullDetailPhoto extends StatefulWidget {
  @override
  _FullDetailPhotoState createState() => _FullDetailPhotoState();
}

class _FullDetailPhotoState extends State<FullDetailPhoto> {
  // Create a text controller and use it to retrieve the current value
  // of the TextField.


  @override
  void dispose() {

    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.deepOrange,
    );
  }
}