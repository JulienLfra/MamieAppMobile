import 'package:flutter/material.dart';

class FullDetailPerso extends StatefulWidget {
  @override
  _FullDetailPersoState createState() => _FullDetailPersoState();
}

class _FullDetailPersoState extends State<FullDetailPerso> {
  // Create a text controller and use it to retrieve the current value
  // of the TextField.


  @override
  void dispose() {

    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.pink,
    );
  }
}